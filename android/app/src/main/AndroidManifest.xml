<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Karaoke: records the singer over the instrumental. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Background playback: react-native-video runs a Media3 MediaSessionService in the foreground -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Reading the user's local music files -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Android 9/10: keep downloads in Music/stash so they survive an uninstall (services/keep.ts) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Floating bubble over other apps (bubble/BubbleManager.kt), only once the user allows it -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Medium / Strong button haptics (haptics/Haptics.kt); granted automatically -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- In-app updates from GitHub Releases (update/UpdaterModule.kt); the user confirms each install -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:requestLegacyExternalStorage="true"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <service
        android:name="com.twg.video.core.services.playback.VideoPlaybackService"
        android:exported="false"
        android:foregroundServiceType="mediaPlayback">
        <intent-filter>
          <action android:name="androidx.media3.session.MediaSessionService" />
        </intent-filter>
      </service>

      <!-- Android Auto: browse screens + now playing, mirroring the app's player (car/CarService.kt) -->
      <meta-data
        android:name="com.google.android.gms.car.application"
        android:resource="@xml/automotive_app_desc" />
      <meta-data
        android:name="com.google.android.gms.car.notification.SmallIcon"
        android:resource="@drawable/ic_launcher_foreground" />
      <service
        android:name=".car.CarService"
        android:exported="true"
        android:foregroundServiceType="mediaPlayback">
        <intent-filter>
          <action android:name="androidx.media3.session.MediaLibraryService" />
          <action android:name="android.media.browse.MediaBrowserService" />
        </intent-filter>
      </service>
      <!-- Cover images for Android Auto (read-only, artwork folder only) -->
      <provider
        android:name=".car.ArtworkProvider"
        android:authorities="${applicationId}.artwork"
        android:exported="true"
        tools:ignore="ExportedContentProvider" />
      <!-- The downloaded update, handed to the system installer -->
      <provider
        android:name=".update.UpdateFileProvider"
        android:authorities="${applicationId}.updates"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/update_paths" />
      </provider>
      <!-- Karaoke recordings, shared with the app the user picks -->
      <provider
        android:name=".karaoke.KaraokeFileProvider"
        android:authorities="${applicationId}.karaoke"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/karaoke_paths" />
      </provider>
    </application>
</manifest>
